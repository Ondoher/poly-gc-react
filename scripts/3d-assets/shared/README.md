# Shared 3D Asset Script Helpers

Shared modules imported by both `../svg-preprocessor/` and
`../asset-pipeline/`.

Keep this folder small: helpers belong here only when both live script groups
need them.
