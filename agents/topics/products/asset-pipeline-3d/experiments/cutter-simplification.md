# Cutter Simplification

Focused cutter-simplification experiments are retained for explicit/manual
follow-up. The focused Experiments app page is currently disabled.

Current production generation uses a dedicated `cutter-2d` stage with the
Paper.js SVG-side `unite-all` simplification before `svg-cutter`.
