# Context

This folder owns bootstrap and routing context for agent sessions.

## Documents

- [Bootstrap](bootstrap.md): the standard context-loading sequence.
- [Routing](routing.md): the topic routing table for the new tree.

The compatibility active-topic file currently lives at:

```text
agents/topics/active-topic.md
```

If the bootstrap contract is later updated, the canonical active-topic file can
move here as `context/active-topic.md`.
