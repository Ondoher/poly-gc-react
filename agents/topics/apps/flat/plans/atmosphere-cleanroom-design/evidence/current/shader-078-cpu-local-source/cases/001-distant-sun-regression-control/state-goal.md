# State Goal

Build and verify the first Node/Three CPU reference layer for Algorithm32 shader work.

The run succeeds when Three camera rays and Raycaster hits can drive Algorithm32 sky/object transfer packets, and when geometry and spectral-transfer identities pass on the generated scene.
