# Unsourced And Partially Sourced Facts

This is the single current ledger for unresolved factual claims in the active
reconciliation lane. It is not a history or evidence index. Chronology belongs
in `tmp/atmosphere/reconciliation/running-log.md`; checked progress belongs in
the reset plan; detailed evidence belongs in immutable numbered records.

Remove a row when its fact is resolved or transferred to a canonical owner.
Replace changed facts in place rather than appending predecessor narratives.

| Id | Area | Current unresolved fact | Next action |
| --- | --- | --- | --- |
| `er5-f04` | Stellar scene policy | CALSPEC Sirius supplies the accepted absolute point-source fixture, but the broader catalog identity, apparent-place accuracy, epoch transforms, and spectrum/passband policy required for the real-scene matrix remain undecided. | Declare the minimum real-star catalog and direction contract before ER6. |
| `er5-f05` | Flat32 synthetic stars | Authored Flat32 star levels and footprints are diagnostic scene facts, not physical irradiance, SEDs, or stellar diameters. Their typed synthetic source policy remains undefined. | Assign an explicit synthetic SED/flux policy or keep each analog diagnostic-only in ER6. |
| `er6-f01` | Display claim | The reset has one global post-composition display boundary, but no calibrated eye, camera, or display-luminance observer is selected. | End the claim at transported pre-display radiance, or explicitly activate ER7 with a sourced observer and background model. |
| `er6-f02` | Near-Moon contact depth | The first calibrated uniform Moon bridge owns exact observer-to-center depth. The accepted extended integrator does not yet derive a separate ray/surface intersection depth for every lunar quadrature sample. | Exclude near-Moon contact/overlap claims in ER6; add per-sample surface depth only if a later scope needs that geometry. |
| `er8-f01` | CPU convergence | Runtime CPU atmosphere and source-integration settings have not yet been bounded against a higher-accuracy reference over the accepted scene matrix. | Execute the ER8 convergence checklist after ER6. |
| `er9-f01` | Solar packet promotion | Production CPU configuration and hardcoded GPU solar values can diverge without one shared quantity-bearing fingerprint. | If promoted in ER9, bind illumination and visible-disk paths to the same immutable solar packet and prove CPU/GPU identity. |
| `promotion-f01` | Ephemeris acquisition | A production browser cannot own direct Horizons acquisition. The application-server validation, serialization, caching, returned-epoch, and provenance contract remains undesigned. | Scope the server boundary only if the promotion decision includes real ephemeris acquisition. |
| `deferred-f01` | Configured local Sun | A physical local Sun still needs declared radius, spectral power, reference distance, falloff, and intent. | Keep deferred until the real distant Sun, globe Moon, and calibrated-star pipeline are complete. |
| `deferred-f02` | Diffuse celestial fields | Milky Way, zodiacal light, and other diffuse fields need calibrated radiance, celestial frame, resampling, and catalog-star double-count policy. | Keep outside Phase 6 and open a separate source-design task if promoted later. |
| `deferred-f03` | Fictional flat Moon | The flat Moon requires one observer-independent shared-world geometry plus explicit material, lighting, phase, and eclipse rules. | Keep deferred until the globe-Moon promotion decision is complete. |
